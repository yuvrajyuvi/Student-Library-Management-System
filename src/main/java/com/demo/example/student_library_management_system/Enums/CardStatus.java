package com.demo.example.student_library_management_system.Enums;

public enum CardStatus {

    // enum - it is a pre-defined data type which contains the constants values for specific fields


    ACTIVE,
    DEACTIVE,
    BLOCKED,
    EXPIRED
}